[project]
name = "jarvis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlx",
    "mlx-lm",
    "mlx-audio",
    "numpy",
    "pydantic",
    "rich",
    "sounddevice",
    "webrtcvad-wheels",
]
